/// Corner radii used across the app, matching the Tailwind `borderRadius` scale.
class AppRadius {
  AppRadius._();

  static const card = 16.0;
  static const lg = 8.0;
  static const full = 999.0;
}

/// Spacing scale, matching the Tailwind `spacing` scale in the mock.
class AppSpacing {
  AppSpacing._();

  static const marginEdge = 24.0;
  static const gutter = 16.0;
  static const unit = 8.0;
  static const stackXs = 4.0;
  static const stackSm = 8.0;
  static const stackMd = 16.0;
  static const stackLg = 24.0;
  static const sidebarWidth = 260.0;
}
